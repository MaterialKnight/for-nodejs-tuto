const n1 = 5
const n2 = 10

function addValues() {
    console.log(`The sum is: ${n1 + n2}`)
}

// Importing a module runs it. When this module is imported (run), the console will show 'The sum is: 15' due to the function call below.
addValues()